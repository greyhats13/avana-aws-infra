[
  {
    "name" : "${unit}-${env}-${code}-${feature}-php",
    "image" : "${image}:latest",
    "cpu" : ${cpu},
    "memory" : ${memory},
    "memoryReservation" : ${memory_reservation},
    "essential" : true,
    "mountPoints"  : [],
    "volumesFrom"  : [],
    "portMappings" : [
      {
        "containerPort" : ${port},
        "hostPort" : ${port},
        "protocol"  : "tcp"
      }
    ]
  }
]